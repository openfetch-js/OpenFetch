---
name: sample-skill
description: Use when the user asks for a minimal example of a Claude Code skill template (structure, frontmatter, or placeholder instructions). Not for production openFetch usage — use the openfetchskill plugin instead.
---

# Sample skill (template)

This file demonstrates the **minimum** useful body after frontmatter.

## What to put here

1. **Constraints** — what the agent must or must not do.
2. **Steps** — ordered checklist for the task.
3. **Examples** — short input/output or code snippets.
4. **Links** — optional paths to `./references/` for long material.

## Optional reference

See [extra notes](./references/extra-notes.md).
